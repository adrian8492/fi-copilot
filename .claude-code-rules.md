# Claude Code Operating Rules — Korum/Benstock Sprint

**Read this file every session before any other work. Re-read after every `/compact`.**

You are operating in autonomous mode for the F&I Co-Pilot Korum + Benstock pilot sprint. These rules are non-negotiable.

---

## 1. Autonomous decision-making

- **Don't ask before proceeding.** Make the best call, log it in `memory/SPRINT-STATE.md`, keep going.
- **Defer all "should I?" questions** to a follow-up phase. Choose the safer reversible option and continue.
- **Never block on a decision Adrian hasn't pre-answered.**
- Output discipline: brief progress notes only. No "should I continue?" — yes, always continue.

## 2. Context management (CRITICAL — prevents freezes near 1M tokens)

- **Compact every 4 hours OR after every major phase**, whichever comes first.
- **Compact summary instruction (paste this when running /compact):**
  > "Preserve: current branch state, last commit hash, in-progress todos, schema decisions made, next phase to start, MySQL dialect note, tenancy.ts patterns, Korum/Benstock context, the spec location at `~/asura-build/content/briefs/fi-copilot-benstock-pilot-sprint.md`, and the rules in `~/fi-copilot/.claude-code-rules.md`. Drop: file read history, test output details, debug exploration."
- **At 70% context** (when warnings appear), immediately:
  1. Commit current work
  2. Push to GitHub
  3. Update `memory/SPRINT-STATE.md`
  4. Run `/compact`
  5. Continue
- **Aggressive tool-result truncation:** only keep summaries from `pnpm test`, `git diff`, large logs. Don't paste full output into context.
- **Read files surgically:** prefer `head`, `tail`, `grep`, or specific line ranges. Full file reads of 2,000+ line files burn 30K+ tokens.

## 3. State persistence (so any session can resume)

After every meaningful unit of work, update `memory/SPRINT-STATE.md` with:
- Current phase + step
- Last commit hash
- In-progress todos
- Decisions made this session (with reasoning)
- Blockers (if any) or "none — proceeding"

This file is the source of truth if a session crashes. Any new session should read it first.

## 4. Commit + push discipline

- Commit + push at every meaningful checkpoint (not just at phase completion)
- Never commit broken code: run `pnpm check` and `pnpm test` before every commit
- If tests drop below baseline (1,299/1,300), STOP and fix before continuing
- If TypeScript errors appear, STOP and fix
- Use descriptive commit messages explaining the *why* (the 213e3a4 commit is a model)

## 5. Defaults when in doubt

| Topic | Default |
|-------|---------|
| Auth | Clerk (already specced) |
| Database | MySQL via Drizzle (DON'T change dialects) |
| Styling | Match existing patterns in the codebase |
| Testing | Write tests inline as you build, never defer |
| Security | Fail-closed — deny if uncertain |
| Spec source | `~/asura-build/content/briefs/fi-copilot-benstock-pilot-sprint.md` |
| Pull frequency | `cd ~/asura-build && git pull` every 2 hours — Oliver updates the spec continuously |

## 6. Sprint priority order

1. Phase 1.5 — sessions.delete tenant fix + compliance_rules semantics
2. Phase 2 — Clerk auth + 5-step onboarding wizard
3. Phase 3 — StoneEagle ingest + `/yesterday-recap` page + Deepgram wire-up on LiveSession.tsx
4. Phase 4 — QA + production deploy

## 7. Stop conditions (these halt the sprint)

- Tests drop below baseline
- TypeScript errors appear
- Real security risk you can't reversibly mitigate
- Sprint is fully complete (Phase 4 done)
- Adrian explicitly says stop

## 8. Recovery if a session crashes

If you're starting fresh after a crash or compaction:
1. Read `memory/SPRINT-STATE.md` first
2. Read `memory/2026-04-24.md` (or the most recent daily journal)
3. Read `~/asura-build/content/briefs/fi-copilot-benstock-pilot-sprint.md`
4. Read recent git log on `feature/multi-tenant-pilot`
5. Pick up where the last session left off
6. No need to re-confirm anything — these rules are still in effect

## 9. Communication discipline

- Don't paste test output to chat — summarize: "1299/1300 passing, 1 pre-existing fail"
- Don't paste file contents — summarize what you found
- Don't ask Adrian or Oliver to confirm obvious next steps — proceed
- Do flag genuine blockers in `memory/SPRINT-STATE.md` and Telegram-via-Oliver if Adrian needs to act

## 10. Korum/Benstock context (always remember)

- **Korum Automotive Group** — Puyallup WA — 7 F&I managers — F&I Director Jim Koch
- **Install: Monday April 27** (3 days from today)
- **Brian Benstock at Paragon Honda follows ~May 1-5**
- **Data source: StoneEagle nightly 2 AM** (first report Tuesday April 28)
- **Critical features: multi-tenant isolation, Clerk auth, onboarding wizard, /yesterday-recap, Deepgram LiveSession transcription**
- **DEEPGRAM_API_KEY already in `.env.local`**

---

**Begin work. Don't reply to this file — execute it.**
